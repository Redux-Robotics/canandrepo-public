# TODO

* move `mkvdepjson.py` into the gradle scripts
* revive `Snippets` et al
* method to get the bus string of a MessageBus
* spotless
* plumbing rust
* more adaptive selection of aarch64-gcc environment and passing that into cargo via `--config`